# Не пиним жёстко: на новых Ubuntu приезжает свежий Python, и старые
# версии pydantic-core под него не собраны — pip уходил компилировать
# Rust и падал. Нижние границы выбраны по первым версиям с готовыми
# колёсами для Python 3.14.
aiogram>=3.31,<4
aiomysql>=0.2,<1
