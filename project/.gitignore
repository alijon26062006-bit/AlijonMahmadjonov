# Logs
logs/*.log

# Uploaded / generated media (keep the folder structure, ignore content)
uploads/posters/*
uploads/banners/*
uploads/trailers/*
uploads/screenshots/*
uploads/thumbnails/*
uploads/backgrounds/*
uploads/tmp/*
!uploads/**/.gitkeep

# Local secrets override (optional). The committed config.php uses safe
# placeholders + env overrides, so keeping it is fine; ignore any local copy.
config/config.local.php

# OS / editor cruft
.DS_Store
Thumbs.db
